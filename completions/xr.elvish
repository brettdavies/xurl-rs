
use builtin;
use str;

set edit:completion:arg-completer[xr] = {|@words|
    fn spaces {|n|
        builtin:repeat $n ' ' | str:join ''
    }
    fn cand {|text desc|
        edit:complex-candidate $text &display=$text' '(spaces (- 14 (wcswidth $text)))$desc
    }
    var command = 'xr'
    for word $words[1..-1] {
        if (str:has-prefix $word '-') {
            break
        }
        set command = $command';'$word
    }
    var completions = [
        &'xr'= {
            cand -X 'HTTP method (GET by default)'
            cand --method 'HTTP method (GET by default)'
            cand -H 'Request headers'
            cand --header 'Request headers'
            cand -d 'Request body data'
            cand --data 'Request body data'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u 'Username for `OAuth2` authentication'
            cand --username 'Username for `OAuth2` authentication'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand -F 'File to upload (for multipart requests)'
            cand --file 'File to upload (for multipart requests)'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add trace header to request'
            cand --trace 'Add trace header to request'
            cand -s 'Force streaming mode'
            cand --stream 'Force streaming mode'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
            cand -V 'Print version'
            cand --version 'Print version'
            cand post 'Post to X'
            cand reply 'Reply to a post'
            cand quote 'Quote a post'
            cand delete 'Delete a post'
            cand read 'Read a post'
            cand search 'Search recent posts'
            cand whoami 'Show the authenticated user''s profile'
            cand user 'Look up a user by username'
            cand timeline 'Show your home timeline'
            cand mentions 'Show your recent mentions'
            cand like 'Like a post'
            cand unlike 'Unlike a post'
            cand repost 'Repost a post'
            cand unrepost 'Undo a repost'
            cand bookmark 'Bookmark a post'
            cand unbookmark 'Remove a bookmark'
            cand bookmarks 'List your bookmarks'
            cand likes 'List your liked posts'
            cand follow 'Follow a user'
            cand unfollow 'Unfollow a user'
            cand following 'List users you follow'
            cand followers 'List your followers'
            cand block 'Block a user'
            cand unblock 'Unblock a user'
            cand mute 'Mute a user'
            cand unmute 'Unmute a user'
            cand usage 'Show API usage (tweet caps, daily breakdown)'
            cand dm 'Send a direct message'
            cand dms 'List recent direct messages'
            cand auth 'Authentication management'
            cand media 'Media upload operations'
            cand skill 'Install or manage the xurl-rs skill bundle'
            cand schema 'Show JSON Schema for a command''s response type'
            cand completions 'Generate shell completion script'
            cand version 'Show xurl version information'
            cand examples 'Print a curated gallery of invocation examples grouped by use case'
            cand validate 'Validate a JSON document against a bundled response schema'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;post'= {
            cand --media-id 'Media ID(s) to attach (repeatable)'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;reply'= {
            cand --media-id 'Media ID(s) to attach (repeatable)'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;quote'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;delete'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --force 'Skip the confirmation prompt; required under `--no-interactive`'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;read'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;search'= {
            cand -n 'Number of results (1-100). Overrides global `--limit` when set'
            cand --max-results 'Number of results (1-100). Overrides global `--limit` when set'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;whoami'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;user'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;timeline'= {
            cand -n 'Number of results (1-100). Overrides global `--limit` when set'
            cand --max-results 'Number of results (1-100). Overrides global `--limit` when set'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;mentions'= {
            cand -n 'Number of results (5-100). Overrides global `--limit` when set'
            cand --max-results 'Number of results (5-100). Overrides global `--limit` when set'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;like'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;unlike'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;repost'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;unrepost'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;bookmark'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;unbookmark'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;bookmarks'= {
            cand -n 'Number of results (1-100). Overrides global `--limit` when set'
            cand --max-results 'Number of results (1-100). Overrides global `--limit` when set'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;likes'= {
            cand -n 'Number of results (1-100). Overrides global `--limit` when set'
            cand --max-results 'Number of results (1-100). Overrides global `--limit` when set'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;follow'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;unfollow'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;following'= {
            cand -n 'Number of results (1-1000). Overrides global `--limit` when set'
            cand --max-results 'Number of results (1-1000). Overrides global `--limit` when set'
            cand --of 'Username to list following for (default: you)'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;followers'= {
            cand -n 'Number of results (1-1000). Overrides global `--limit` when set'
            cand --max-results 'Number of results (1-1000). Overrides global `--limit` when set'
            cand --of 'Username to list followers for (default: you)'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;block'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;unblock'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;mute'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;unmute'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;usage'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;dm'= {
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;dms'= {
            cand -n 'Number of results (1-100). Overrides global `--limit` when set'
            cand --max-results 'Number of results (1-100). Overrides global `--limit` when set'
            cand --auth 'Authentication type (oauth1, oauth2, app)'
            cand -u '`OAuth2` username to act as'
            cand --username '`OAuth2` username to act as'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -t 'Add X-B3-Flags trace header'
            cand --trace 'Add X-B3-Flags trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
            cand oauth2 'Configure `OAuth2` authentication'
            cand oauth1 'Configure `OAuth1` authentication'
            cand app 'Configure app-auth (bearer token)'
            cand status 'Show authentication status'
            cand clear 'Clear authentication tokens'
            cand apps 'Manage registered X API apps'
            cand default 'Set default app and/or user'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;auth;oauth2'= {
            cand --no-browser 'Enable manual two-step flow for headless machines (SSH, containers)'
            cand --step 'Step number: 1 (generate auth URL) or 2 (complete exchange)'
            cand --auth-url 'Redirect URL from browser (step 2). Use ''-'' to read from stdin (recommended on shared machines)'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;oauth1'= {
            cand --consumer-key 'Consumer key'
            cand --consumer-secret 'Consumer secret'
            cand --access-token 'Access token'
            cand --token-secret 'Token secret'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;app'= {
            cand --bearer-token 'Bearer token'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;status'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;clear'= {
            cand --oauth2-username 'Clear `OAuth2` token for username'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --all 'Clear all authentication'
            cand --oauth1 'Clear `OAuth1` tokens'
            cand --bearer 'Clear bearer token'
            cand --force 'Skip the confirmation prompt; required under `--no-interactive`'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;apps'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
            cand add 'Register a new X API app'
            cand update 'Update credentials for an existing app'
            cand remove 'Remove a registered app'
            cand list 'List registered apps'
            cand redirect-uri 'Inspect or set the stored `OAuth2` redirect URI for an app'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;auth;apps;add'= {
            cand --client-id '`OAuth2` client ID'
            cand --client-secret '`OAuth2` client secret'
            cand --redirect-uri '`OAuth2` redirect URI (https or http on loopback)'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;apps;update'= {
            cand --client-id '`OAuth2` client ID'
            cand --client-secret '`OAuth2` client secret'
            cand --redirect-uri '`OAuth2` redirect URI (https or http on loopback); empty string clears'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;apps;remove'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --force 'Skip the confirmation prompt; required under `--no-interactive`'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;apps;list'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;apps;redirect-uri'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
            cand get 'Show the effective redirect URI, its source, and the stored value'
            cand set 'Set the stored redirect URI for an app (empty string clears)'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;auth;apps;redirect-uri;get'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;apps;redirect-uri;set'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;apps;redirect-uri;help'= {
            cand get 'Show the effective redirect URI, its source, and the stored value'
            cand set 'Set the stored redirect URI for an app (empty string clears)'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;auth;apps;redirect-uri;help;get'= {
        }
        &'xr;auth;apps;redirect-uri;help;set'= {
        }
        &'xr;auth;apps;redirect-uri;help;help'= {
        }
        &'xr;auth;apps;help'= {
            cand add 'Register a new X API app'
            cand update 'Update credentials for an existing app'
            cand remove 'Remove a registered app'
            cand list 'List registered apps'
            cand redirect-uri 'Inspect or set the stored `OAuth2` redirect URI for an app'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;auth;apps;help;add'= {
        }
        &'xr;auth;apps;help;update'= {
        }
        &'xr;auth;apps;help;remove'= {
        }
        &'xr;auth;apps;help;list'= {
        }
        &'xr;auth;apps;help;redirect-uri'= {
            cand get 'Show the effective redirect URI, its source, and the stored value'
            cand set 'Set the stored redirect URI for an app (empty string clears)'
        }
        &'xr;auth;apps;help;redirect-uri;get'= {
        }
        &'xr;auth;apps;help;redirect-uri;set'= {
        }
        &'xr;auth;apps;help;help'= {
        }
        &'xr;auth;default'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;auth;help'= {
            cand oauth2 'Configure `OAuth2` authentication'
            cand oauth1 'Configure `OAuth1` authentication'
            cand app 'Configure app-auth (bearer token)'
            cand status 'Show authentication status'
            cand clear 'Clear authentication tokens'
            cand apps 'Manage registered X API apps'
            cand default 'Set default app and/or user'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;auth;help;oauth2'= {
        }
        &'xr;auth;help;oauth1'= {
        }
        &'xr;auth;help;app'= {
        }
        &'xr;auth;help;status'= {
        }
        &'xr;auth;help;clear'= {
        }
        &'xr;auth;help;apps'= {
            cand add 'Register a new X API app'
            cand update 'Update credentials for an existing app'
            cand remove 'Remove a registered app'
            cand list 'List registered apps'
            cand redirect-uri 'Inspect or set the stored `OAuth2` redirect URI for an app'
        }
        &'xr;auth;help;apps;add'= {
        }
        &'xr;auth;help;apps;update'= {
        }
        &'xr;auth;help;apps;remove'= {
        }
        &'xr;auth;help;apps;list'= {
        }
        &'xr;auth;help;apps;redirect-uri'= {
            cand get 'Show the effective redirect URI, its source, and the stored value'
            cand set 'Set the stored redirect URI for an app (empty string clears)'
        }
        &'xr;auth;help;apps;redirect-uri;get'= {
        }
        &'xr;auth;help;apps;redirect-uri;set'= {
        }
        &'xr;auth;help;default'= {
        }
        &'xr;auth;help;help'= {
        }
        &'xr;media'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
            cand upload 'Upload media file'
            cand status 'Check media upload status'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;media;upload'= {
            cand --media-type 'Media type (e.g., video/mp4)'
            cand --category 'Media category (e.g., `amplify_video`)'
            cand --auth 'Authentication type'
            cand -u 'Username'
            cand --username 'Username'
            cand -H 'Request headers'
            cand --header 'Request headers'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --wait 'Wait for media processing to complete'
            cand -t 'Trace header'
            cand --trace 'Trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;media;status'= {
            cand --auth 'Authentication type'
            cand -u 'Username'
            cand --username 'Username'
            cand -H 'Request headers'
            cand --header 'Request headers'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand -w 'Wait for processing'
            cand --wait 'Wait for processing'
            cand -t 'Trace header'
            cand --trace 'Trace header'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;media;help'= {
            cand upload 'Upload media file'
            cand status 'Check media upload status'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;media;help;upload'= {
        }
        &'xr;media;help;status'= {
        }
        &'xr;media;help;help'= {
        }
        &'xr;skill'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
            cand install 'Install the skill bundle into a host''s canonical skills directory'
            cand update 'Refresh an existing skill-bundle install in place'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;skill;install'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --all 'Install into every known host in one invocation'
            cand --dry-run 'Print the resolved git command without spawning'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;skill;update'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --all 'Update every known host in one invocation'
            cand --dry-run 'Print the resolved plan without removing or cloning'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;skill;help'= {
            cand install 'Install the skill bundle into a host''s canonical skills directory'
            cand update 'Refresh an existing skill-bundle install in place'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;skill;help;install'= {
        }
        &'xr;skill;help;update'= {
        }
        &'xr;skill;help;help'= {
        }
        &'xr;schema'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --list 'List all commands and their response types'
            cand --all 'Output all schemas as a single JSON document'
            cand --envelope 'Output the canonical agent-native output envelope schema'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;completions'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;version'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;examples'= {
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;validate'= {
            cand --schema 'Schema name to validate against (`tweet`, `tweets`, `user`, `users`, `dm`, `dms`, `usage`, `envelope`). Omit for auto-detection'
            cand -v 'Print verbose information'
            cand --verbose 'Print verbose information'
            cand --app 'Use a specific registered app (overrides default)'
            cand --output 'Output format. text (default), json, jsonl, ndjson (alias of jsonl), yaml (`.yml`), csv, tsv. Formats not in the value enum (e.g. toml, xml) are not supported — xurl emits a JSON envelope with reason `invalid-args` if requested'
            cand --raw 'Emit unstyled, compact output. Strips ANSI in text mode; compact (no pretty-printing) JSON in json/jsonl modes'
            cand -q 'Suppress all non-essential output (errors still go to stderr)'
            cand --quiet 'Suppress all non-essential output (errors still go to stderr)'
            cand --no-interactive 'Disable interactive prompts; fail with error instead'
            cand --timeout 'Request timeout in seconds'
            cand --color 'Colorize output: auto (TTY-aware), always, or never'
            cand --dry-run 'Validate inputs and skip the API call (U7)'
            cand --limit 'Global result-set limit, clamped to 1..=100 (U7)'
            cand --cursor 'Pagination cursor / `pagination_token` for list endpoints'
            cand --page 'Documented alias for `--cursor`'
            cand --after 'Documented alias for `--cursor` (`--after <token>`)'
            cand --json 'Shorthand for `--output json` (P2 alias)'
            cand --jsonl 'Shorthand for `--output jsonl` (P2 alias)'
            cand --no-pager 'Documented no-op. `xr` writes directly to stdout and never invokes `$PAGER`; this flag is advertised so agents can pass `--no-pager` unconditionally without xr rejecting it'
            cand -h 'Print help (see more with ''--help'')'
            cand --help 'Print help (see more with ''--help'')'
        }
        &'xr;help'= {
            cand post 'Post to X'
            cand reply 'Reply to a post'
            cand quote 'Quote a post'
            cand delete 'Delete a post'
            cand read 'Read a post'
            cand search 'Search recent posts'
            cand whoami 'Show the authenticated user''s profile'
            cand user 'Look up a user by username'
            cand timeline 'Show your home timeline'
            cand mentions 'Show your recent mentions'
            cand like 'Like a post'
            cand unlike 'Unlike a post'
            cand repost 'Repost a post'
            cand unrepost 'Undo a repost'
            cand bookmark 'Bookmark a post'
            cand unbookmark 'Remove a bookmark'
            cand bookmarks 'List your bookmarks'
            cand likes 'List your liked posts'
            cand follow 'Follow a user'
            cand unfollow 'Unfollow a user'
            cand following 'List users you follow'
            cand followers 'List your followers'
            cand block 'Block a user'
            cand unblock 'Unblock a user'
            cand mute 'Mute a user'
            cand unmute 'Unmute a user'
            cand usage 'Show API usage (tweet caps, daily breakdown)'
            cand dm 'Send a direct message'
            cand dms 'List recent direct messages'
            cand auth 'Authentication management'
            cand media 'Media upload operations'
            cand skill 'Install or manage the xurl-rs skill bundle'
            cand schema 'Show JSON Schema for a command''s response type'
            cand completions 'Generate shell completion script'
            cand version 'Show xurl version information'
            cand examples 'Print a curated gallery of invocation examples grouped by use case'
            cand validate 'Validate a JSON document against a bundled response schema'
            cand help 'Print this message or the help of the given subcommand(s)'
        }
        &'xr;help;post'= {
        }
        &'xr;help;reply'= {
        }
        &'xr;help;quote'= {
        }
        &'xr;help;delete'= {
        }
        &'xr;help;read'= {
        }
        &'xr;help;search'= {
        }
        &'xr;help;whoami'= {
        }
        &'xr;help;user'= {
        }
        &'xr;help;timeline'= {
        }
        &'xr;help;mentions'= {
        }
        &'xr;help;like'= {
        }
        &'xr;help;unlike'= {
        }
        &'xr;help;repost'= {
        }
        &'xr;help;unrepost'= {
        }
        &'xr;help;bookmark'= {
        }
        &'xr;help;unbookmark'= {
        }
        &'xr;help;bookmarks'= {
        }
        &'xr;help;likes'= {
        }
        &'xr;help;follow'= {
        }
        &'xr;help;unfollow'= {
        }
        &'xr;help;following'= {
        }
        &'xr;help;followers'= {
        }
        &'xr;help;block'= {
        }
        &'xr;help;unblock'= {
        }
        &'xr;help;mute'= {
        }
        &'xr;help;unmute'= {
        }
        &'xr;help;usage'= {
        }
        &'xr;help;dm'= {
        }
        &'xr;help;dms'= {
        }
        &'xr;help;auth'= {
            cand oauth2 'Configure `OAuth2` authentication'
            cand oauth1 'Configure `OAuth1` authentication'
            cand app 'Configure app-auth (bearer token)'
            cand status 'Show authentication status'
            cand clear 'Clear authentication tokens'
            cand apps 'Manage registered X API apps'
            cand default 'Set default app and/or user'
        }
        &'xr;help;auth;oauth2'= {
        }
        &'xr;help;auth;oauth1'= {
        }
        &'xr;help;auth;app'= {
        }
        &'xr;help;auth;status'= {
        }
        &'xr;help;auth;clear'= {
        }
        &'xr;help;auth;apps'= {
            cand add 'Register a new X API app'
            cand update 'Update credentials for an existing app'
            cand remove 'Remove a registered app'
            cand list 'List registered apps'
            cand redirect-uri 'Inspect or set the stored `OAuth2` redirect URI for an app'
        }
        &'xr;help;auth;apps;add'= {
        }
        &'xr;help;auth;apps;update'= {
        }
        &'xr;help;auth;apps;remove'= {
        }
        &'xr;help;auth;apps;list'= {
        }
        &'xr;help;auth;apps;redirect-uri'= {
            cand get 'Show the effective redirect URI, its source, and the stored value'
            cand set 'Set the stored redirect URI for an app (empty string clears)'
        }
        &'xr;help;auth;apps;redirect-uri;get'= {
        }
        &'xr;help;auth;apps;redirect-uri;set'= {
        }
        &'xr;help;auth;default'= {
        }
        &'xr;help;media'= {
            cand upload 'Upload media file'
            cand status 'Check media upload status'
        }
        &'xr;help;media;upload'= {
        }
        &'xr;help;media;status'= {
        }
        &'xr;help;skill'= {
            cand install 'Install the skill bundle into a host''s canonical skills directory'
            cand update 'Refresh an existing skill-bundle install in place'
        }
        &'xr;help;skill;install'= {
        }
        &'xr;help;skill;update'= {
        }
        &'xr;help;schema'= {
        }
        &'xr;help;completions'= {
        }
        &'xr;help;version'= {
        }
        &'xr;help;examples'= {
        }
        &'xr;help;validate'= {
        }
        &'xr;help;help'= {
        }
    ]
    $completions[$command]
}
